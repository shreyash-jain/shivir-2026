{
  "name": "attendance-app",
  "version": "1.0.0",
  "private": true,
  "description": "Android shell around the static scanner in ../scanner. build_www.sh copies it and bakes in the server details; nothing else is added.",
  "scripts": {
    "www": "sh build_www.sh",
    "sync": "sh build_www.sh && cap sync android",
    "apk": "sh build_www.sh && cap sync android && cd android && ./gradlew assembleDebug",
    "release": "sh build_www.sh && cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
